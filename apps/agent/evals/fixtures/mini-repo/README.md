# Mini Repo

A tiny fixture project used by the Steer agent eval goldens. It exists so the
read-only tools (`list_dir`, `read_file`, `grep`, `glob`) have real files to
operate on. It is not a real package.
