/**
 * App shell placeholder. U6 builds the real two-pane shell (sidebar + session
 * detail) behind the WorkOS login gate, following design-reference/DESIGN-SYSTEM.md.
 */
export function App(): JSX.Element {
  return <div className="app-scaffold">steer — UI scaffold (U6 builds the shell)</div>
}
